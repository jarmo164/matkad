<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Matkaklubi - <%= matk.nimetus %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="bootstrap.css">
    <link rel="stylesheet" href="matkaklubi.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <%- include('components/header') %>
        <div class="matk-detail mt-4">
            <div class="row">
                <div class="col-lg-12 mb-4">
                    <a href="/" class="btn btn-secondary">← Tagasi</a>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-6">
                    <img src="<%= matk.pildiUrl %>" alt="<%= matk.nimetus %>" class="img-fluid rounded">
                </div>
                <div class="col-lg-6">
                    <h1 class="mb-3"><%= matk.nimetus %></h1>
                    <p class="lead"><%= matk.kirjeldus %></p>
                    <div class="mt-4">
                        <h3>Osalejad</h3>
                        <ul class="list-group">
                            <% matk.osalejad.forEach(osaleja => { %>
                            <li class="list-group-item"><%= osaleja %></li>
                            <% }); %>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

